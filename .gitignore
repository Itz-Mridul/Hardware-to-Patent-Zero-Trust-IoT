# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/

# Virtual environments
.venv/
venv/
env/

# Databases (runtime data, not source)
*.db
!tests/attack_data.db

# macOS
.DS_Store
.AppleDouble
.LSOverride

# IDE
.vscode/
.idea/
*.swp
*.swo

# Logs
logs/
*.log

# TLS Certificates (generated — do not commit private keys to git)
certs/*.key
certs/*.csr
certs/*.srl
# DO commit: certs/*.crt and certs/README.md (public certs are safe)

# Environment / secrets
.env
*.env

# Model binaries (trained on device — not portable)
pi_backend/fingerprint_model.pkl
*.h5
*.pkl

# Original data
edge_ml_gateway/iot_data.db
iot_data.db

# RAM vault (volatile keys — never persist)
/mnt/vault_keys/

# Firmware build artifacts
*.bin
*.elf

# Hardware config (contains secrets)
config.h
config.json

# Blockchain local data
ganache_db/

# Node.js (Truffle/Hardhat)
node_modules/
build/
